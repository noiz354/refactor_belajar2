package norman.hackerrank;

import norman.template.Template;

/**
 * @author normansyahputa on 7/12/17.
 *
 * first it suspends because i need better understanding of DP of Tree.
 */
public class KingdomDivisionMyOwn extends Template {
    public KingdomDivisionMyOwn() {
        super("KingdomDivision", "KingdomDivision", LINUX, true);
    }

    @Override
    public void doSomething() {
        int N = getInput().nextInt();

        // ada n-1 line lagi
        for (int i = 0; i < N - 1; i++) {
            int a = getInput().nextInt();
            int b = getInput().nextInt();


        }

    }
}
